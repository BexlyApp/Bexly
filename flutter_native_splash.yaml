flutter_native_splash:
  # Use brand color for splash — looks good in both light and dark system themes.
  # Native splash can't follow app theme (runs before Flutter engine).
  color: "#00B4AB"
  image: assets/icon/Bexly-logo-no-bg.png

  android_12:
    color: "#00B4AB"
    image: assets/icon/Bexly-logo-no-bg.png
    icon_background_color: "#00B4AB"

  android: true
  ios: true
  web: true
