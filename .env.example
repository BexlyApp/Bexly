# Copy this file to .env and add your actual API keys
# NEVER commit the .env file to version control!

# Groq API Key from https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini API Key from https://console.cloud.google.com/
# Used for AI Chat and Receipt OCR (Gemini 2.5 Flash)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key from https://platform.openai.com/api-keys
# Used for Receipt OCR (GPT-4o Vision) - Optional
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Claude API Key from https://console.anthropic.com/
# Used for Receipt OCR (Claude Sonnet 4) - Optional
CLAUDE_API_KEY=your_claude_api_key_here

# Bexly Free Tier AI (DOS AI - self-hosted)
# Used for free users who don't have their own API keys
BEXLY_FREE_AI_URL=https://api.dos.ai/v1
BEXLY_FREE_AI_KEY=your_bexly_free_ai_key_here
BEXLY_FREE_AI_MODEL=Qwen/Qwen3-VL-30B-A3B-Instruct

# DOS-Me Supabase (Authentication)
# Get these from Supabase Dashboard > Settings > API
SUPABASE_URL=https://dos.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key_here

# Google OAuth Android Client IDs (for native Google Sign In on Android)
# Get these from Google Cloud Console > APIs & Services > Credentials (dos-me project)
# Use the ANDROID OAuth 2.0 Client IDs (NOT Web client ID)
# Each build type needs its own client ID because SHA-1 fingerprints are different:
# - Debug client needs debug keystore SHA-1 (keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android)
# - Release client needs release keystore SHA-1 (keytool -list -v -keystore your-release.jks -alias your_alias)
GOOGLE_ANDROID_CLIENT_ID_DEBUG=your_debug_client_id_here.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID_RELEASE=your_release_client_id_here.apps.googleusercontent.com